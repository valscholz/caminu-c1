depthai>=3.5,<4
faster-whisper>=1.0
kokoro-onnx>=0.3
openwakeword>=0.6
webrtcvad>=2.0
sounddevice>=0.4
numpy<2
requests>=2.31
Pillow>=10
opencv-python-headless>=4.8
# ReSpeaker Mic Array v3 XMOS tuning interface (DOA for follow-up gating).
pyusb>=1.2
# Memory embedder. We deliberately avoid sentence-transformers because it
# pulls in torch which drags a conflicting CUDA 13 runtime that breaks
# Kokoro's CUDA provider on JetPack 6.2 (which is CUDA 12.6).
fastembed>=0.4
